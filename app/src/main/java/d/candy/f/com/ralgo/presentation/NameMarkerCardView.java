package d.candy.f.com.ralgo.presentation;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.RelativeLayout;

/**
 * Created by daichi on 8/19/17.
 */

public class NameMarkerCardView extends RelativeLayout {

    public NameMarkerCardView(Context context) {
        super(context);
        init(context);
    }

    public NameMarkerCardView(Context context, AttributeSet attrs) {
        super(context, attrs);
        init(context, attrs);
    }

    private void init(Context context) {}

    private void init(Context context, AttributeSet attrs) {
        init(context);
    }
}
